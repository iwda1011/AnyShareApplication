<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="@dimen/activity_vertical_margin">


    <TextView
        android:text="Neueste Veranstaltungen in deiner Nähe"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="24dp"
        android:id="@+id/textView" />

    <ListView
        android:layout_width="350dp"
        android:layout_height="200dp"
        android:id="@+id/LVNews"
        android:background="@drawable/border"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Veranstaltung suchen"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:background="@drawable/my_button_border"
        android:id="@+id/veransuchen"
        android:layout_below="@+id/LVNews"
        android:layout_alignStart="@+id/veranserstellen"
        android:layout_marginTop="24dp" />

    <Button
        android:text="Veranstaltung erstellen"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:id="@+id/veranserstellen"
        android:background="@drawable/my_button_border"
        android:layout_below="@+id/veransuchen"
        android:layout_alignStart="@+id/profil"
        android:layout_marginTop="30dp" />

    <Button
        android:text="Profil"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:id="@+id/profil"
        android:background="@drawable/my_button_border"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="19dp" />

</RelativeLayout>
